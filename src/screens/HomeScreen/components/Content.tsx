import React from 'react';
import { View, StyleSheet } from 'react-native';

export function Content() {
  return (
    <View style={styles.content}>
      {/* Contenu principal à implémenter */}
    </View>
  );
}

const styles = StyleSheet.create({
  content: {
    flex: 1,
    padding: 16,
  },
});